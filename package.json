{
  "devDependencies": {
    "babel-core": "^6.24.0",
    "babel-loader": "^6.4.1",
    "babel-preset-env": "^1.3.2",
    "exports-loader": "^0.6.4",
    "webpack": "^2.3.3"
  },
  "scripts": {
    "build": "webpack -p && npm run html && npm run styles && npm run images",
    "html": "cp index.html favicon.png dist/",
    "styles": "cp -R styles/ dist/styles/",
    "images": "cp -R images/ dist/images/",
    "start": "cd dist && http-server",
    "deploy": "s3cmd put --guess-mime-type --no-mime-magic --acl-public --exclude .DS_Store  --recursive --add-header=\"Cache-Control: max-age=31536000\" ./dist/ s3://ryanming.com/"
  },
  "dependencies": {
    "p5": "^0.5.8"
  }
}
