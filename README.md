[ryanming.com](ryanming.com)

First `yarn install` to get all the dependencies.

`yarn run build` to create a build of the website in the `dist` folder.
`yarn run start` run a webserver out of the `dist` folder.

